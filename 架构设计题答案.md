# 架构设计题答案

对于以下大模型生成的答案，我进行了检查和校对，然后每个模块选择了回答较好的一题呈现。

---

## 模块一：Context / Performance

### 题目 1：大模型面对第一轮长窗口或多模态输入时，first token 会显著变慢。有什么快速/低成本/用户体验也不差的方案？从 5-10 秒稳定压缩到 2 秒。

**答案：**

先说清楚根因：首轮慢的本质是 **prefill 阶段**要一次性对所有输入 token（长文本或图像 patch 都有海量 token）做 `QKV` 自注意力计算，计算量与输入长度呈超线性/二次方关系，所以 TTFT（Time To First Token）被拉长，而 decode 阶段则逐 token 产出，相对稳定。因此优化的核心是"**减小首轮 prefill 的 token 量**"和"**复用已经算过的 KV**"，而不是去优化 decode。

可落地的方案（按性价比排序）：

1. **Prompt 前缀缓存（Prefix Caching）**——最有效
   - Agent 场景里系统提示、工具 Schema、few-shot 示例、历史对话都是**高度稳定**的前缀。把"公共前缀"的 KV cache 缓存命中后，首轮只需对新输入做增量 prefill。
   - 例如首轮第一次请求看图 5 秒，第二次同类请求命中缓存可压到 1-2 秒。成本几乎为零，体验无损失。vLLM、SGLang 等都原生支持。
   - 落地要点：把"最稳定、最长"的内容放最前面（system prompt → tools → 历史 → 新 query），最大化前缀命中率。

2. **多模态输入先压缩 / 降采样，再进模型**
   - 图片在送模型前做**分辨率缩放 + 动态 patch 裁剪**：降低图像 token 数（一张 4K 图可能占上千 token）。
   - 视频/长文档则先抽帧、先做 OCR/摘要，只喂关键帧/关键段，而不是全部塞入。
   - 用户体验不差的原因：视觉问答对分辨率不敏感的场景（如"这是什么物品"）完全可以降采样。

3. **用检索替代"全量长窗口"（RAG）**
   - 长上下文不必整段入参。先用 embedding 检索出与当前问题最相关的 chunk 注入，把首轮输入从"整篇文档"降到"几百 token 的相关片段"。
   - 代价是可能漏上下文，需保留"全量兜底"开关。

4. **预热常驻实例 + 提前 prefill（Prefetch）**
   - 用户一进入会话就后台预热连接、keep-alive 常驻实例，避免冷启动排队。
   - 更激进一点：在用户输入定格/点击前，就把"可预测前缀"提前算好（例如已知 system + 前几轮历史），用户一发送只需要 prefill 新的一句话。

5. **Chunked Prefill / 推理侧优化**
   - 换用支持 chunked prefill 的推理引擎，把长 prefill 拆块与 decode 交错，可明显降低 TTFT（尤其长输入）。这偏基础设施层，但属于"低成本"。

6. **减少输入冗余**
   - 精简 system prompt、去掉重复内容、压缩历史（见题目 2 的分层摘要），从源头减 token。

**取舍总结**：前缀缓存 + 输入压缩 + 检索裁剪组合，几乎不损害用户体验、成本低，通常就能把 5-10 秒稳定压到 2 秒以内，是首选；chunked prefill 和常驻预热作为工程兜底。

---

### 题目 2：一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？

**答案：**

核心思想：**不要"一律摘要"，而要"分层分级"**——把上下文分成"必须逐字保留的硬信息"和"可以概括的软信息"，再按预算动态取舍。

具体做法：

1. **滑动窗口 + 滚动摘要（层次化压缩）**
   - 保留**最近 N 轮原文**（如最近 10-20 轮），保证局部连贯和 pronoun 指代不丢。
   - 更早的历史用 LLM 压缩成**滚动摘要（rolling summary）**，且是**增量式**：每次只把"新跨越窗口的内容"合并进旧摘要，而不是每轮重做全文摘要（省 token、省延迟）。

2. **区分"事实/状态"与"闲聊"**
   - 硬信息：用户偏好、未完成任务、约束条件、关键数值、决策结论、待办——这些必须**结构化、逐字保留**，不能进模糊摘要。
   - 软信息：寒暄、重复讨论、过程性解释——可以概括甚至丢弃。
   - 落地：用两套存储，**结构化状态（任务状态机 + 用户画像）** 与**对话摘要**分离。

3. **Token 预算管理**
   - 设定总预算（如 80% 满触发压缩），按 **recency + relevance + importance** 加权评分，只保留最相关的片段；对早期信息做**检索式压缩**（需要时再召回，而非常驻）。

4. **触发时机**
   - 达到阈值（如接近 context 上限的 80%）时主动触发，而非等到溢出报错，避免截断导致的对话断裂。

**如何确保压缩后仍流畅：**

- **摘要必须保留"指代与实体"**：人名、产品名、代词指代对象，否则后续对话会"牛头不对马嘴"。
- **摘要要求输出结构化字段**：`[事实] [任务/待办] [用户偏好] [未决问题] [近期结论]`，并在指令里明确要求模型"保留可被后续引用的具体信息，只压缩叙述"。
- **保留最近几轮原文**：刚聊的内容用户记忆最清晰，压缩这部分会显得"失忆"，只压远处。
- **关键硬信息独立于摘要常驻**：任务状态、用户约束永远在 context 里，不依赖摘要的"概括质量"。
- **必要时让压缩可追溯**：摘要对应到原始轮次范围，后续若需要精确信息可"解压/回查"（配合检索召回）。

一句话：**远的历史 → 结构分化（摘要 + 画像 + 状态），近的历史 → 原文，中间 → 加权保留**，这样既控预算又不丢流畅。

---

## 模块二：Memory

### 题目 1：和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？

**答案：**

半个月前的信息早就不在短期上下文里了，这题的实质是**长期记忆的"写入"和"召回"**要协同设计。

**召回侧（更关键）：**

1. **按需召回（Just-in-time），而非全量塞入**
   - 不要把所有 memory 常驻 context。每次用户提问时，用当前 query 触发检索，只注入 top-k 最相关的记忆（如 3-5 条）+ 分层概览。
   - 好处：省 token、避免了无关记忆干扰模型。

2. **混合检索，而不是纯向量**
   - **语义检索**（embedding 相似度）：解决"换个说法问同一件事"。
   - **关键词/BM25 精确检索**：人名、产品名、专有名词不能靠语义猜，必须精确命中。
   - **结构化过滤**：按时间、主题、会话、标签过滤（如"半个月内""关于某项目"）。
   - 混合后统一打分排序。

3. **多级召回 + 分级注入**
   - 先召回"概览级"（用户画像/偏好，几乎总是相关），再按 query 召回"情景级"（episodic 具体问答）。
   - 属于经典的**短期/长期分层**：半个月前 → 命中长期层。

4. **排序加权：recency + relevance + importance**
   - 时间衰减（太久远降低），但要警惕重要性高的记忆不该被时间压没（如"永远不要给我发语音"类偏好）。

5. **召回结果要可辨识 + 防幻觉**
   - 命中时让 Agent 知道这是"记忆回放"（如加 `[memory]` 标记），并在回答里自然带出"你之前提过…"，便于用户判断对不对。
   - 设相似度阈值，低于阈值不强行memory，避免生造。

**写入侧：**

- 在对话中识别"值得长期记住"的信息（用户偏好、事实、决策、反复出现的主题）再写入，而不是无脑全存。
- 写入时打时间戳、主题标签、来源会话，方便日后过滤和溯源。
- 支持去重与更新（用户改了偏好要覆盖旧的，而非堆叠新旧矛盾）。

**参考实现**：Mem0、Letta（MemGPT）、Zep 等基本都走"结构化提取 → 存储 → 混合检索 → 按需注入 → 自更新"这套。

---

### 题目 2：你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？

**答案：**

**经典框架：**

1. **认知科学分层**
   - **短期记忆（Working Memory / Short-Term）**：即 context window，容量有限、易失。
   - **长期记忆（Long-Term）**：持久化存储，需写入/检索机制。
   - 工程上通常再加**中期记忆**（会话内滚动摘要）作为缓冲。

2. **内容类型分类**
   - **情景记忆（Episodic）**：具体发生过的事件/对话流水。
   - **语义记忆（Semantic）**：提炼出的稳定事实、用户画像、领域知识。
   - **程序性记忆（Procedural）**：技能/流程/SOP、规则。

3. **核心组件（生命周期）**
   - 写入（Encode/Extract）→ 存储（Store，向量库 + 图 + 结构化 DB）→ 检索（Retrieve，混合检索）→ 整合（Integrate，注入 context）→ **反思/巩固（Reflect/Consolidate）** → **更新/遗忘（Update/Forget）**。
   - **MemGPT / Letta** 的影响很大：提出"virtual context"，把 memory 当操作系统的分页来管理（main context 与 external context 之间交换），并让 Agent **自编辑记忆**，而非被动存储。

**发展趋势：**

1. **从单一向量库 → 混合存储检索**：向量 + 知识图谱 + 结构化事实 + 全文检索并用，解决纯向量"精确事实易错、时序关系弱、关系推理差"的短板。
2. **检索式按需注入，取代"塞满 context"**：越长的上下文越需要"召回"，而不是"全给"。
3. **Agent 自编辑 + 反思（Reflection）**：记忆不再是死存储，Agent 会主动总结、合并、修正，形成越来越精炼的画像。
4. **Memory-as-a-Service / 记忆层解耦**：记忆作为独立服务/层，跨会话、跨 Agent、跨应用共享。
5. **结构化与可验证**：减少让 LLM 自由生成的不可靠摘要，转向结构化事实 + 可追溯来源。
6. **时间感知、遗忘机制、权限与隐私隔离**：记忆也有"过期"和多租户权限问题。

**头部玩家在怎么做：**

- **OpenAI（ChatGPT memory）**：跨对话记忆用户偏好，"记住/忘记"显式控制，记忆可管理；方向上在做 context 工程与工具化记忆。
- **Anthropic（Claude Projects）**：用 Project 级长期上下文/memory + 用户自定义规则沉淀。
- **Google（Gemini）**：主打超长上下文 + 会话记忆。
- **专用赛道**：Mem0（结构化+抽取+图检索）、Letta（虚拟上下文/自编辑）、Zep（时序神经网络检索 + 事实/对话分离）、LangChain 的 LangMem（写入/检索/更新的框架化）。
- **Coding Agent（Cursor / Windsurf / Claude Code）**：把 memory 固化为仓库里的 `rules` / `memory` 文件 + 项目上下文，强调"可被用户审查和版本控制"。

**一句话趋势**：从"记忆=把历史塞进 prompt"，走向"记忆=结构化事实 + 混合检索 + 自更新 + 服务化解耦"。

---

## 模块三：Task

### 题目 1：对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺点？

**答案：**

问题本质：长任务跨多轮、多工具调用，context 里早期目标被后续中间结果"冲淡"，模型注意力漂移。解决思路分几类：

1. **目标/任务状态持续注入（最直接）**
   - 每轮循环都把 `goal + 当前阶段 + 已完成 + 剩余约束` 作为结构化状态重新塞回 prompt。
   - 优点：实现简单、稳定可控。缺点：占 token；状态写得好坏依赖人。

2. **任务状态机 / Checklist（Todo List）**
   - 任务初始就被分解成显式的步骤清单，每完成一步"打勾"更新状态，模型始终看得到"现在到哪了、还差什么"。
   - 优点：结构清晰、可追踪、防漂移。缺点：分解本身可能不准，需动态增删子步骤（re-plan）；状态更新要保证一致。

3. **层次化规划（Plan-and-Execute / 子目标分解）**
   - 顶层做长期规划（子目标），执行器每次只专注**当前子目标**，不背全盘；偏离时触发 re-plan。
   - 优点：天然抗遗忘（每次只局部思考）、易并行。缺点：规划开销、规划错误会放大、重规划时可能丢失已完成成果。

4. **外部记忆 / Scratchpad + 检索**
   - 关键中间结果、目标、约束写入外部 memory，需要时按需召回，不常驻 context。
   - 优点：省 token、支持超长任务。缺点：召回可能漏、检索不准会导致又"忘"。

5. **定期反思 / 自检（Reflection / Self-Verify）**
   - 每 N 步显式让模型对照目标自问"当前行动是否仍在朝目标推进？"，输出进度与偏离预警。
   - 优点：能及时发现漂移并纠偏。缺点：额外 LLM 调用带来成本和延迟；反思也可能"自我感觉良好"。

6. **外部验证器 / Critic**
   - 独立的 verifier 检查中间结果是否满足目标/约束，不合格则回退重做。
   - 优点：客观、可复用（可用规则/测试）。缺点：验证器本身要能定义"对错"，很多开放式任务难以自动判定。

7. **确定性编排 / 工作流（Workflow）**
   - 用代码把任务拆成确定性步骤图（DAG），LLM 只做局部决策，流程和状态由代码掌控。
   - 优点：稳定、可重试、不遗忘。缺点：灵活性下降，通用自然语言任务难以完全建模。

**权衡**：工程上通常是"任务状态机 + 状态持续注入"打底，长任务再叠加"层次化规划 + 外部记忆 + 定期反思"。

---

### 题目 2：用户给 Agent 下达任务：每天早上 9 点根据昨天聊天情况做复盘总结。你会怎么设计？

**答案：**

这是一道"定时触发 + 数据获取 + 长文本总结 + 推送"的综合设计题。分层设计如下：

1. **任务解析与注册**
   - 把自然语言调度需求解析成结构化定时任务：`cron: 0 9 * * *`、`时区: 用户本地`、`频次: daily`、`输入: 昨日会话`、`输出: 复盘总结`。
   - 存入任务表，支持后续修改时间、内容、暂停/取消。

2. **调度触发（Scheduler）**
   - 用可靠的调度组件：APScheduler / Celery Beat / Temporal 等，到点触发一个 job。
   - 关键：**按用户时区**计算"昨天"（`用户时区 昨天 00:00 ~ 23:59`），不能直接套服务器时区。

3. **数据获取（Fetcher）**
   - 从 session 日志/消息存储中拉取该用户"昨天"的全部聊天记录。
   - 边界处理：昨天无数据时兜底（如产出一句"昨日无对话记录"或跳过并留存状态）；多会话要按会话分组。

4. **复盘总结编排（Pipeline）**
   - 昨天聊天可能很长、多会话，直接用单次 LLM 会超 context。用 **map-reduce 分层摘要**：
     - `map`：把每个会话/长对话切块，分别总结出关键点。
     - `reduce`：把所有块级小结合并成一份完整复盘。
   - 结构化输出模板，例如：`[关键话题] [任务/事项进展] [待办] [用户情绪/偏好变化] [风险/未决问题]`。

5. **写入 Memory + 投递**
   - 复盘结果落入 memory/数据库，沉淀为"每日复盘"时间线，供日后召回。
   - 通过 channel（应用内通知 / 消息推送 / 邮件）在 9 点后把复盘推给用户。

6. **可靠性与一致性（重点）**
   - **幂等**：同一天只生成一次，用 `(user_id, date)` 唯一键去重，避免重跑产生重复复盘。
   - **失败重试**：job 失败可重试，带退避；跨天补跑要能兜底。
   - **可观测**：记录每次执行的日志、耗时、结果状态。
   - **权限**：只读取该用户本人的数据，涉及隐私需脱敏。

7. **组件全景**
   - `Scheduler（触发）` → `Fetcher（取数）` → `Summarizer（map-reduce 总结）` → `Store（持久化）` → `Notifier（推送）`，外加 `State/幂等控制` 和 `配置管理`。

---

## 模块四：Tool / Session Runtime

### 题目 1：Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？

**答案：**

核心是把一次 tool call 建模成"**提交 → 任务句柄 → 完成回调**"的异步生命周期，而不是阻塞等结果。

1. **任务抽象（提交/查询分离）**
   - 异步工具调用拆成两步：`submit` 立即返回 `task_id + 状态(accepted/queued/running)`，随后结果通过 `poll / callback` 异步回传。
   - 立即给用户一个中间反馈："已提交，完成后我会继续"——不让用户干等。

2. **执行层：任务队列**
   - 异步任务投递到消息队列 / 任务系统（Temporal、Celery、内置队列），worker 执行，runtime 不再阻塞。
   - 任务状态机：`pending → running → succeeded / failed / timed_out`。

3. **完成通知（两种，推荐组合）**
   - **事件驱动（首选）**：webhook / SSE / WebSocket，工具完成后主动把事件（带 `task_id + result`）推回。
   - **轮询兜底**：runtime 定期 query 工具状态，防止回调丢失。
   - 关键：完成事件必须能**精确定位回当初那个会话和那次 tool call**（靠 `task_id` / `session_id` 关联），这样 runtime 能"唤醒"被挂起的 Agent 继续跑。

4. **Runtime 侧：挂起与恢复（Resume）**
   - 异步工具 call 发出后，把该 session 标记为 `awaiting_tool` 并释放资源（挂起）。
   - 完成事件到达时，把 `result` 回填到那一步 tool call，恢复 Agent 循环继续执行。这是异步工具能"结果依然重要"的关键——不是给用户看一眼，而是能把结果喂回 agent 继续处理。

5. **可靠性与边界**
   - **超时**：给异步任务设 TTL，超时标记失败并走兜底。
   - **重试与幂等**：失败重试；完成事件可能重复投递，用 `task_id` 幂等去重。
   - **持久化**：任务 + 结果落库（事件溯源 / outbox pattern），进程崩溃后能恢复，不丢结果。

6. **用户体验闭环**
   - 用户侧：先看到"任务已受理"的即时反馈，完成后再收到最终结果（或 Agent 基于结果继续的后续动作），避免"提交后杳无音讯"。

---

### 题目 2：如果 session state 为 busy，此时用户又发来新消息，或者异步工具完成事件也到达，runtime 应该如何处理？

**答案：**

本质是**单会话上的并发事件与状态机一致性问题**。最安全的解法是"串行化 + 显式状态机 + 幂等关联"。

1. **显式状态机（先定义清楚状态）**
   - `idle / running(busy) / awaiting_tool / waiting_user / finished / error` 等，明确每个状态允许接收哪些事件。

2. **事件邮箱 / 队列化（Actor 模型思路）**
   - 每个 session 一个**单线程事件循环（event loop）**，所有入站事件（用户消息、工具完成、系统事件）都进入 per-session 的 FIFO 队列，runloop 一次只处理一个事件。
   - 这样从根上避免多线程并发写同一个 session state 的竞态，是推荐做法。

3. **busy 时用户新消息如何处理**
   - 默认**排队**：进入事件队列，等当前回合结束后再消费；同时给用户一个"正在处理中，消息已受理"的提示。
   - 支持**打断/抢占（Cancel & Restart）**：如果新消息明显是要推翻当前指令，可触发"取消当前运行 → 用新指令重建上下文继续"。打断要处理好：已发出的工具调用结果回来后如何归位（通常直接丢弃或落日志，标记该次调用已作废）。

4. **异步工具完成事件到达（且此时 session 又 busy）如何处理**
   - 完成事件先入队，通过 `task_id` 关联回**对应的挂起调用**。
   - 分情况：
     - 属于当前正在 await 的调用 → 正常推进该步。
     - 属于**已取消/超时/过期**的调用 → 丢弃或仅落日志，避免把陈旧结果混进当前 flow。
   - 幂等处理：同一 `task_id` 只应用一次结果，防重复触发。

5. **优先级与丢弃策略**
   - 给事件类型设优先级（如"用户取消/打断" > "新用户消息" > "工具完成" > "定时/系统事件"）。
   - 队列过长或超时，给出合并/丢弃策略，避免队列无限堆积。

6. **一致性来源**
   - 根本保障：**单 runloop 串行消费 + 状态持久化（事件溯源）**，保证可恢复、可回放、可审计。这也解释了为何 LangGraph、OpenHands 等 runtime 都采用事件循环/事件流驱动。

**一句话**：busy 时不盲目并发，用"每会话一个邮箱 + 显式状态机 + task_id 幂等关联"来串行化事件，用户消息默认排队、支持打断，工具完成事件按 task_id 归位、过期即丢弃。

---

## 模块五：Agent Runtime 架构对比

### 题目 1：Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？它们各自这样设计的优缺点是什么？

**答案：**

两者最大的差异在于**工具调用与结果如何结构化地在"模型 ↔ 运行时"之间传递**。

**Claude Code（Anthropic）的方式：**
- 使用**结构化消息块** `tool_use`（发起调用，含 `id + name + input`）和 `tool_result`（返回结果，含 `tool_use_id + content`），结果是**内容块（content block）**而非纯文本。
- 一条 assistant 消息可同时包含**多个 tool_use 块**，天然支持并行调用。
- 工具结果作为携带 `tool_use_id` 的 block **累积在对话历史里**，模型能完整再看、再引用；结果可表达 `error` 标志，也能承载多 part / 多模态内容。
- 典型的是 Agent Harness：工具多在本地由 harness/claude 执行，模型只负责决策。

**OpenAI-compatible function calling（GLM / 豆包等）的方式：**
- assistant 输出 `function_call`（`name + arguments`，参数是 **JSON 字符串**），开发者自行执行后，以 **`role=tool`** 消息回填，内容是 `tool_call_id + content`（**纯字符串**）。
- 调用和结果都走"文本/字符串"这一统一通道。

**各自优缺点：**

| | Claude Code 式（结构化 block） | OpenAI-compatible 式（function calling） |
|---|---|---|
| 表达力 | 强：可表达错误、多 part、并行、多模态结果 | 弱：结果只能是一段字符串，复杂结构需自我约定/序列化 |
| 并行与关联 | 天然，一条消息多 `tool_use`，`id` 关联清晰 | 支持，但依赖 `tool_call_id` 在消息流里配对 |
| 生态/兼容 | 绑定 Anthropic 格式，跨模型成本高 | 事实标准，几乎所有模型/框架支持，迁移成本低 |
| 实现复杂度 | 运行时需处理更丰富的消息结构 | 简单统一，容易实现与调试 |
| agent 亲和度 | 贴合复杂 agent（结果可回看、可错误语义） | 适合标准工具调用，复杂场景表达吃力 |

**一句话总结**：Claude Code 用更强结构化的 block 换取复杂 Agent 场景的表达能力与可回放性，但牺牲生态通用性；OpenAI-compatible 用统一的字符串/JSON 换取简单与兼容，但表达力弱。国内 GLM/豆包走 OpenAI-compatible 路线，拥抱生态是其优势，但在复杂 agent 语义（错误、并行、长结果、多模态结果）上需要在协议之上自己约定。

---

### 题目 2：OpenHands 的状态机设计有什么优缺点？更优雅的实现方式是怎么样的？

**答案：**

**OpenHands（原 OpenDevin）的设计：**
- 以**事件流（Event Stream）驱动**：核心是 Action / Observation（Event），AgentController 主循环运行 Agent，事件在 controller、sandbox runtime、前端之间流转。
- `AgentState` 用扁平**枚举**描述状态：如 `LOADING / RUNNING / AWAITING_USER_INPUT / FINISHED / ERROR / PAUSED` 等，配合 State 里的 plan、error 等字段。
- sandbox 与运行环境隔离在 Docker runtime 里，工具/命令执行通过事件进出。

**优点：**
1. **事件溯源 / 可回放**：所有 Action/Observation 都是事件，天然支持重放、审计、断点恢复、调试。
2. **解耦**：前端、controller、runtime 通过事件解耦，易于扩展多端和并发。
3. **可观测性强**：事件流天然是执行 trace，便于监控与排查。

**缺点：**
1. **扁平枚举的状态机表达力不足**：一旦涉及并发、暂停/恢复、超时、用户打断、异步工具完成等场景，就需要大量 `状态 × 事件` 的特判，容易组合爆炸、写出一堆 if/else。
2. **强耦合事件 schema**：新增 action 就要改事件格式，扩展有成本。
3. **竞态处理麻烦**：事件驱动下，cancel/stop、并发事件到达时的状态一致性需要额外小心。
4. **复杂度随功能增长**：事件流本身会变得很重，维护成本上升。

**更优雅的实现方式：**

1. **Actor 模型 + 邮箱队列**
   - 每个 session/agent 一个 Actor，靠**串行邮箱**处理消息，天然消除并发竞态，状态成为 Actor 内部状态，输入输出都是消息（Akka / Elixir / Actix 思路）。这直接解决"扁平状态机 + 并发事件"的最大痛点。

2. **分层 / 正交状态机（Statecharts）**
   - 用 Harel statechart / XState 把状态分层、正交化：`运行` 和 `暂停/等待/错误` 等作为正交维度并行组合，避免组合爆炸；显式定义 transition、guard、并行子状态。

3. **Durable Execution（持久化执行 / Temporal 式）**
   - 把 Agent 执行建模为持久化 workflow：崩溃恢复、超时、重试、信号（用户打断=signal）都由引擎兜底，状态不是内存枚举而是可恢复的执行历史。这是长任务的"更高一层"解答。

4. **函数式 Reducer 模式**
   - `state = reducer(state, event)`，单一不可变状态 + 纯函数转换 + 事件溯源（Redux 思路），状态转换可预测、可回放、易测试。

**核心升级点**：把"扁平枚举状态机"升级为"分层状态图"，把"事件广播"升级为"每会话串行邮箱队列的 Actor"，把"内存状态"升级为"事件溯源 / durable execution"——三者结合比 OpenHands 的扁平 event + 枚举更抗并发、更易扩展、更可恢复。